{
  "name": "functions",
  "version": "1.0.0",
  "description": "CAKU Erasmus Cloud Functions - Server-side authentication",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.1.0"
  }
}
