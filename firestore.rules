rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tüm koleksiyonlar açık - uygulama kendi şifre sistemiyle güvenliği sağlıyor
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
